<h1>All orders<h1>

<% for order in @orders %>
    <div class="order">
        <li><strong><%= order.listing.title %></strong></li>
        <li><%= order.created_at %></li>
        <li><%= order.user.address %></li>
    </div>
    <br>
<% end %>